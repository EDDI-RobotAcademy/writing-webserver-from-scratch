package com.eddicorp.quiz.week2.servlet.http;

/**
 * HTTP Method를 표현합니다.
 */
public enum HttpMethod {
    OPTIONS,
    GET,
    POST,
    PUT,
    DELETE,
    HEAD,
    TRACE,
    CONNECT,
    PATCH
}
